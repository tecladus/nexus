# Dependencias del proyecto NEXUS

# Para criptografía (Ed25519, SHA-256)
cryptography>=42.0.0

# Servidores HTTP (orquestador, minero)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Cliente HTTP async
httpx>=0.27.0

# Tests
pytest>=8.0.0
